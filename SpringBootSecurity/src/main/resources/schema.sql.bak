create table users(
	username varchar_ignorecase(50) not null primary key,
	password varchar_ignorecase(500) not null,
	enabled boolean not null
);

create table authorities (
	username varchar_ignorecase(50) not null,
	authority varchar_ignorecase(50) not null,
	constraint fk_authorities_users foreign key(username) references users(username)
);
create unique index ix_auth_username on authorities (username,authority);


INSERT INTO users(username, password, enabled) VALUES ('Walter De Keyser', '{noop}password', true);
INSERT INTO users(username, password, enabled) VALUES ('Willy De Keyser', '{noop}password', true);
INSERT INTO users(username, password, enabled) VALUES ('Ken De Keyser', '{noop}password', true);

INSERT INTO authorities(username, authority) VALUES ('Walter De Keyser', 'ROLE_USER');
INSERT INTO authorities(username, authority) VALUES ('Willy De Keyser', 'ROLE_USER');
INSERT INTO authorities(username, authority) VALUES ('Willy De Keyser', 'ROLE_ADMIN');
INSERT INTO authorities(username, authority) VALUES ('Ken De Keyser', 'ROLE_USER');